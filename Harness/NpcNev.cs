using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NpcNev : Navigation
{

    // Start is called before the first frame update
    new void Start()
    {
        base.Start();
        tagName = "npcDestination";

    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
}
